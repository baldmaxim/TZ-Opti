# Строгий режим integration-тестов (npm run verify:integration).
# Без TEST_DATABASE_URL тесты не пропускаются, а падают — чтобы «зелёный» прогон
# в CI не означал «интеграция просто не выполнялась».
TEST_DB_REQUIRED=1
